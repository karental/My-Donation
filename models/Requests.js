class Request {
    constructor(id, title, location, description, contactInfo) {
        {
            this.id = id;
            this.title = title;
            this.location = location;
            this.description = description;
            this.contactInfo = contactInfo;
        };
    }
}

export default Request;